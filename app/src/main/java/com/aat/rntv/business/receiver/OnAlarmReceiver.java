package com.aat.rntv.business.receiver;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

/**
 * Created by Refael Ozeri on 3/11/16.
 */
public class OnAlarmReceiver extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {
//        WakefulIntentService.acquireStaticLock(context);
//
//        Enums.TeaType teaType = (Enums.TeaType) intent.getSerializableExtra(TeamiConstants.teaTypeEnum);
//
//        Intent intentService = new Intent(context, AppService.class);
//        intentService.putExtra(TeamiConstants.teaTypeEnum, teaType);
//
//        context.startService(intentService);
    }
}